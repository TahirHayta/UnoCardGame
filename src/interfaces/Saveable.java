package interfaces;

public interface Saveable {

	/**
	 * saves a class by writing necessary information to a file.
	 * @param filename is the file information will be written.
	 */
	public void save(String filename);
}
